// Write a program to find whether the array of integers contains a duplicate number.

#include<stdio.h>
int main()
{
    int n;
    printf("Enter the no. of elements you want to add in an array: ");
    scanf("%d",&n);
    int arr[n],i,dup;
    for(i=0;i<n;i++)
    {
        
    }
    return 0;
}